<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/fond"
    >
    <LinearLayout style="@style/TitleBar">
		<ImageButton style="@style/TitleBarAction"
			android:contentDescription="@string/description_home" android:src="@drawable/ic_title_home"
			android:onClick="goHome" />
		<ImageView style="@style/TitleBarSeparator" />
		<TextView style="@style/TitleBarText" android:text="@string/app_name" android:ellipsize="marquee"/>
		<ProgressBar style="@style/TitleBarProgressIndicator"
			android:id="@+id/title_refresh_progress" android:visibility="gone" />
		<ImageView style="@style/TitleBarSeparator" />
		<ImageButton style="@style/TitleBarAction" android:id="@+id/btn_title_search"
			android:src="@drawable/ic_title_search" android:onClick="onSearchClick" />
		<ImageView style="@style/TitleBarSeparator" />
		<ImageButton style="@style/TitleBarAction" android:id="@+id/btn_title_share"
			android:src="@drawable/ic_title_share" android:onClick="onShareClick" />
	</LinearLayout>
    <FrameLayout android:layout_height="fill_parent"
        android:layout_width="fill_parent"
        android:layout_marginTop="2dip"
        android:background="@android:color/transparent"
        >
        <ListView  android:id="@android:id/list"
            android:layout_height="wrap_content" android:layout_width="fill_parent" 
            android:scrollingCache="false"
            
            android:background="@android:color/transparent"
            />
		<TextView android:textSize="22dip" android:background="@android:color/transparent" android:text="@string/loading" android:isScrollContainer="true" android:layout_height="fill_parent" android:gravity="left" android:layout_width="fill_parent" android:id="@android:id/empty" android:scrollbars="vertical"></TextView>
    </FrameLayout>
</LinearLayout>